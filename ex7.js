//Rewrite this 'if' using the ternary operator '?':

    if (a + b < 4) {
        result = 'Below';
      } else {
        result = 'Over';
      } 

//solution
rezult = (a + b < 4) ? 'Below':'Over';
//or
(a + b < 4) ? 'Below':'Over';